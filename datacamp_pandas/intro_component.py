""" 
DataFrames consist of 3 different components, accessible using attributes.

df.values
-> contains the data value in a 2-dimensional NumPy array

df.columns
-> contains column names, and the index attribute contains row numbers or row numbers

df.index
-> contains row numbers or row names
* row labels ate stored in dot-index, NOT in dot-rows
** this allows for flexibility in labels
""" 