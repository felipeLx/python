""" 
DataFrame()
Pandas have a lot of methods.

df.head()
-> returns the first few rows of the DataFrame

df.info()
-> display the names of columns, the data types that contains, and whether they have any missing values

df.shape
-> contains a tuple that holds the number of rows followed by the number of columns
(rows, columns)

df.describe()
-> computes some summary statistics for numerical columns, like mean and median
like:
    count:
    mean:
    std:
    min:
    25%:
    50%:
    75%:
    max:
 """