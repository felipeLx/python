# linear boundaries
    # w1x1 + w2x2 + b = 0
    # x as the input / w as the weight / b as the bias

# vectorized implementation
    # Wx + b = 0
    # W = (w1, w2)
    # x = (x1, x2)
    # y label 0 or 1
# score = 2*Test + Grade - 18
# prediction:
    # score > 0 Accept
    # score < 0 Reject

# score 7 to grade 6
# 2 x 7 + 6 - 18 = score

# 3D dimension
# w1x1 + w2x2 + w3x3 + b = 0
# W:(1xn), x:(nx1), b:(1x1)
